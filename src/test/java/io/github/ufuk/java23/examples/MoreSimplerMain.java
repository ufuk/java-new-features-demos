void main() {
    // Deprecated version: `println(message());`
    // Requires `IO` utility class as prefix since Java 25
    IO.println(message());
}

String message() {
    return "Bye-bye boilerplate!";
}